for using `zsh` first you need to install it:
```
sudo apt install zsh
```

then go to `oh my zsh` website and use `curl` or `wget` link to download the `ohmyzsh` framework; You can read its document from its github to understand better. 
Also can make it default shell and use beautiful "random theme".
