درمورد الگوریتم طبقه‌بندی شده (Classification algorithm) می‌خواهیم صحبت کنیم.
به طور خلاصه می‌توان گفت که این نوع الگوریتم برای داده‌های با خروجی گسسته است. می‌توانند عدد باشند یا حتی عدد نباشند. 
برای پیش‌بینی دسته‌بندی‌ها استفاده می‌شود. 
