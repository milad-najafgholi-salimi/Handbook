Example:
```
SELECT 1 = 1;

# output:
t # stands for True
```

Example:
```
SELECT 1 = 2;

# output:
f # stands for False
```

And all other operators like `>=`, `<=`, `<>` (stands for not);

Example:
```
SELECT 1 <> 2;
# output: t
```
