Return only unique (only one time) of something that I want.

### Syntax:
```
SELECT DISTINCT something FROM table_name ORDER BY something;
```

For example:
```
SELECT DISTINCT country_of_birth FROM person ORDER BY country_of_birth;
```
